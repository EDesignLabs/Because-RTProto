// Generated by CoffeeScript 1.6.1

define(function() {
  var D3View;
  return D3View = Backbone.View.extend({
    _ensureElement: function() {
      var $el, attrs, _ref;
      if (!this.el) {
        attrs = _.extend({}, _.result(this, 'attributes'));
        if (this.id) {
          attrs.id = _.result(this, 'id');
        }
        if (this.className) {
          attrs["class"] = _.result(this, 'className');
        }
        this.d3el = (_ref = this.options.svg) != null ? _ref.append(_.result(this, 'tagName')) : void 0;
        this.d3el.attr(attrs);
        $el = $(this.d3el.node());
        return this.setElement($el, false);
      } else {
        return this.setElement(_.result(this, 'el'), false);
      }
    }
  });
});
