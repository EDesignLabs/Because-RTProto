// Generated by CoffeeScript 1.4.0

define(['toolbar-view', 'add-view', 'metadata-view'], function(ToolbarView, AddView, MetadataView) {
  var ControlView;
  return ControlView = Backbone.View.extend({
    className: 'span12',
    initialize: function(options) {
      Backbone.View.prototype.initialize.call(this, options);
      return this.dispatcher = options.dispatcher;
    },
    render: function(options) {
      this.metadataView = new MetadataView({
        model: this.model.get('data'),
        dispatcher: this.dispatcher,
        el: this.$el.find('.metadata')
      });
      this.toolbarView = new ToolbarView({
        model: this.model.get('context'),
        dispatcher: this.dispatcher,
        el: this.$el.find('.toolbar')
      });
      this.addView = new AddView({
        model: this.model.get('context'),
        dispatcher: this.dispatcher,
        el: this.$el.find('.add')
      });
      this.metadataView.render();
      this.toolbarView.render();
      return this.addView.render();
    }
  });
});
