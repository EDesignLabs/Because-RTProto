// Generated by CoffeeScript 1.4.0

define(function() {
  var DescriptionView;
  return DescriptionView = Backbone.View.extend({
    initialize: function(options) {
      Backbone.View.prototype.initialize.call(this, options);
      this.dispatcher = options.dispatcher;
      return this.model.addEventListener(gapi.drive.realtime.EventType.TEXT_INSERTED, _.bind(this.onDescriptionChanged, this));
    },
    render: function(options) {
      return this.$el.text(this.model.getText());
    },
    onDescriptionChanged: function(rtEvent) {
      return this.$el.text(this.model.getText());
    }
  });
});
